function solution(n, r, c) {
    var answer = 0;
    const arr = [[]];
    for (let index = 1; index < n; index++) {
        const lineArr = [];
        for (let index = 1; index < n; index++) {

        }
        arr.push([]);
    }
    console.log(arr);
    // 가로 기준으로
    // 1, 2, 6, 7, 15, 16
    // 3
    // 4
    // 10



    return answer;
}

solution(5, 3, 2);